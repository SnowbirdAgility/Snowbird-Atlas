# Snowbird Atlas Site

Static local test site for the Snowbird Atlas landing page.

## Run locally

```powershell
npm run dev
```

Then open:

```text
http://127.0.0.1:4173
```

If that port is already in use, the server prints the next available local URL.

You can also open `index.html` directly in a browser.
